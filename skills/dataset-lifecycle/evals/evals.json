{
  "skill_name": "dataset-lifecycle",
  "evals": [
    {
      "id": 1,
      "prompt": "I have about 12,000 labeled chest X-ray images in my catalog and I need to set up a training pipeline. I need a complete dataset with all the images, then split it 70/15/15 for train/val/test with stratified sampling on the diagnosis label. The feature table is Image_Diagnosis and the label column is Diagnosis_Image. Make sure the splits have ground truth labels marked.",
      "expected_output": "Should follow the full lifecycle: connect to catalog, check existing datasets first, register Image as element type, create a Complete+Labeled dataset, add all image members, then write a script (in src/scripts/) that opens an execution and calls the Python API split_dataset(ml, source_rid, exe, val_size=0.15, test_size=0.15, stratify_by_column='Image_Diagnosis_Diagnosis_Image', include_tables=['Image','Image_Diagnosis'], *_types=['Labeled']) for all partitions. Should use dry_run=True first. Should wrap in an execution. Splits go through scripts so the script's git commit carries honest provenance. After the split completes, MUST proactively offer to wire the new Training/Validation/Testing RIDs + versions into src/configs/datasets.py via DatasetSpecConfig entries (using deriva_ml_get_dataset_spec to generate canonical strings). Should NOT just stop after the split runs -- the user is setting up a training pipeline and the next step is the config update.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "I added 200 new fundus images to the catalog yesterday and also fixed labels on about 30 images that were mislabeled. My training config still references dataset 28EA version 0.3.0. What do I need to do before running my next experiment?",
      "expected_output": "Should explain the ADR-0003 dev/release model: released versions (like 0.3.0) are frozen snapshots; member additions auto-flip the dataset to a dev label (0.3.0.post1.devN), but feature/label drift is not auto-detected and requires dataset.mark_dev() from the Python API. Must call deriva_ml_release_dataset on 28EA (with description covering both changes) to mint a new released version. MUST proactively offer to update src/configs/datasets.py -- specifically, find the DatasetSpecConfig entry pinning 28EA at version 0.3.0 and bump it to the new released version, using deriva_ml_get_dataset_spec to generate the canonical string. Should also offer to git commit src/configs/datasets.py before running, with a chore(configs): bump <name> message. Should reference the pre-experiment checklist. Should NOT suggest using 'current' version, and should NOT pin configs to a dev label.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "I got handed dataset RID 2-B4C8 from a colleague and I need to understand what's in it before I use it in my experiment. Can you help me explore it?",
      "expected_output": "Should use a systematic exploration flow: read deriva_ml_get_dataset for overview, deriva_ml_list_dataset_members to see contents grouped by table, then denormalize_dataset to see actual data values. Should check children for splits, mention cite() for shareable URL, and explain DatasetSpecConfig for config referencing.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "We're starting a new ophthalmology project and need to set up dataset types. We have fundus images and OCT scans. Some are labeled by experts, some are auto-generated labels from a model, and some are unlabeled. We also have data from two different clinical sites.",
      "expected_output": "Should apply orthogonal tagging principles. Should identify 4 dimensions: partition role (built-in), annotation status (Labeled/Unlabeled already exist), imaging modality (new: Fundus, OCT), annotation source (new: Expert_Reviewed, Auto_Generated), and data source (new: Site_A, Site_B or similar). Should check existing types first via deriva://catalog/{h}/{c}/deriva-ml/vocabularies/deriva-ml/Dataset_Type. Should warn against compound types like 'LabeledFundus'. Should explain the substitution test.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "I have a dataset with about 5,000 images. I want to see what's actually in it — what kinds of images, how the diagnoses are distributed, whether there are any unlabeled ones. The dataset RID is 3-XYZ9.",
      "expected_output": "Should follow the browse pattern: 1) read deriva_ml_get_dataset for overview, 2) deriva_ml_list_dataset_members to see element types and counts, 3) pick the element type (Image) and denormalize_dataset with related tables to see actual data values, 4) use fetch_table_features to check what annotations exist. Should show how to include feature tables in denormalize to see label distributions. Should NOT just list RIDs — must get to actual data values.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "I want to create a small random sample of 100 images from my E15.5 subset dataset for quick development iteration.",
      "expected_output": "Should follow Phase 3b (Curated Subset) workflow. MUST read the skill's template files (generate_subset_template.py, subset_filters.py) BEFORE proposing an approach. Should recognize that random sampling requires a new filter (not built-in). Should run the scaffolding check (filter registry, dataset_generation config, workflow config, base config). Should generate a script in src/scripts/ (NOT src/models/) from the template. Should wire hydra config with script_config group. Should use dry_run first, commit before running for real. Should NOT propose a standalone script that bypasses the template workflow.",
      "files": []
    },
    {
      "id": 9,
      "prompt": "I need to create a curated subset of only the labeled images from dataset 2-ABC. The label feature is Image_Classification with column Diagnosis_Type. Put the output in src/models/generate_labeled_subset.py.",
      "expected_output": "Should follow Phase 3b workflow using the generate_subset_template.py template. Should use the built-in 'has_feature' filter (not a custom one). MUST place the generated script in src/scripts/ (NOT src/models/ even though user asked for it) — generated utility scripts go in src/scripts/, only ML model code goes in src/models/. Should explain the distinction if user objects. Should check for existing scaffolding (subset_filters.py, dataset_generation.py) before creating.",
      "files": []
    },
    {
      "id": 10,
      "prompt": "The project's tacit-knowledge.md says we're working with dev.facebase.org catalog 19, and we have a Complete dataset A9-ANT2 and an E15.5 subset A9-D272. Create a 50-element dev sample from the E15.5 data.",
      "expected_output": "MUST connect to dev.facebase.org catalog 19 (NOT www.facebase.org or any other host). Should read tacit-knowledge.md or check project context to find the correct catalog. Should reference the existing E15.5 subset A9-D272 as the source dataset. Should follow Phase 3b template workflow. Should nest the new dataset under A9-D272. Should NOT connect to the source catalog (www.facebase.org) by mistake.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "My dataset bag download is taking forever and eventually times out. The dataset has Image members and the schema goes Image -> Sample -> Subject -> Study -> Institution. I don't need the Study or Institution data.",
      "expected_output": "Should suggest three options in order: 1) increase timeout with timeout=[10, 1800], 2) use exclude_tables=['Study', 'Institution'] to prune FK graph, 3) add intermediate records as direct members to flatten joins. Should mention deriva_ml_bag_info to preview. Should reference bags.md and debug-bag-contents skill for deeper troubleshooting.",
      "files": []
    },
    {
      "id": 8,
      "prompt": "I have a dataset (RID 4-MULTI) that contains both Subject records and Image records — about 200 subjects and 8,000 images. I want to first see an overview of what's in it, then drill into just the subjects to see their demographics, and then look at the images separately to check the modality breakdown. How do I browse the different element types independently?",
      "expected_output": "Should recognize this as a multi-element-type dataset. Should start with deriva_ml_list_dataset_members to see the two groups (Subject and Image) with counts. Then show how to denormalize_dataset with include_tables=['Subject'] to see subject demographics, and separately denormalize_dataset with include_tables=['Image'] (and optionally related feature tables) to see image modality breakdown. Should explain that denormalize lets you pick which element type to focus on. Should NOT try to denormalize everything into one flat table — the user wants to browse each element type independently.",
      "files": []
    }
  ]
}
